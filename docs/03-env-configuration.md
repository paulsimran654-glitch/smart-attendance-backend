# DAY-3 Progress – Smart Attendance Management System (Backend)

## Tasks Completed
- Created environment configuration using dotenv.
- Added MongoDB URI and JWT secret to `.env`.
- Refactored backend code to remove hardcoded credentials.
- Implemented secure configuration loading from environment variables.

## Features Implemented
- **Environment Variable Configuration**
  - Sensitive configuration values are now stored in `.env`.

- **Secure Backend Setup**
  - Improved backend security by eliminating hardcoded secrets.